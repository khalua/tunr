%h1= "Purchasing #{@song.name}"
%h3 Mixtape
= form_tag("/songs/#{@song.id}/buy/")
- Mixtape.all.each do |mixtape|
  = mixtape.name
  = check_box_tag 'song[mixtape_ids][]',mixtape.id, mixtape.in?(@song.mixtapes)
