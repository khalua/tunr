.row
  .five.columns
    = render(:partial => 'shared/errors', :locals => {:object => @genre})
    = form_for @genre do |f|
      = f.label :name
      = f.text_field :name
      = f.submit :class => 'button radius'
      %br